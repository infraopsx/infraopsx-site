---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
import Header from '../../../../components/Header.astro';
import Footer from '../../../../components/Footer.astro';
import KubernetesResourceCalculator from '../../../../components/tools/KubernetesResourceCalculator.astro';
---

<BaseLayout
  title="Kubernetes Resource Calculator | InfraOpsX"
  description="用于规划 Kubernetes 工作负载 requests、limits、可调度节点容量和 Pod 密度的浏览器工具。"
  lang="zh-CN"
  canonicalPath="/zh/tools/kubernetes-resource-calculator/"
  enPath="/tools/kubernetes-resource-calculator/"
  zhPath="/zh/tools/kubernetes-resource-calculator/"
  pagefindType="Tool"
>
  <Header locale="zh" switchHref="/tools/kubernetes-resource-calculator/" />

  <main class="tool-detail-shell" data-pagefind-body>
    <div class="container tool-detail-container">
      <div class="tools-breadcrumb" data-pagefind-ignore>
        <a href="/zh/">首页</a>
        <span>/</span>
        <a href="/zh/tools/">工具</a>
        <span>/</span>
        <span>Kubernetes Resource Calculator</span>
      </div>

      <header class="tool-detail-header">
        <span class="section-label">KUBERNETES · 资源规划</span>
        <h1>Kubernetes Resource Calculator</h1>
        <p>为同构 Kubernetes 节点池规划工作负载 requests 和节点容量。</p>
      </header>

      <section class="tool-section" aria-labelledby="calculator-heading">
        <div class="tool-section-heading">
          <span class="section-label">计算器</span>
          <h2 id="calculator-heading">在部署前规划 requests。</h2>
        </div>
        <div data-pagefind-ignore>
          <KubernetesResourceCalculator locale="zh" />
        </div>
      </section>

      <section class="tool-info-grid">
        <article class="tool-info-card">
          <span class="section-label">工作原理</span>
          <h2>将 requests 与可调度容量进行比较。</h2>
          <p>Kubernetes 调度通常依据 effective resource requests。这个计算器使用已配置的 requests；如果没有配置 request 但配置了 limit，本次计算会将该 limit 作为 effective request。计算器会将 CPU 统一为 mCPU、Memory 统一为 bytes，在每个节点上应用规划预留，再将 effective workload requests 与计算后的可调度容量进行比较。15% 是 InfraOpsX Calculator 的容量规划默认值，不是 Kubernetes 或 kubelet 的默认 systemReserved。该值仅用于容量规划，不代表 kubelet 的 systemReserved、kubeReserved 或实际 Node Allocatable。已配置的 limits 也会作为规划和 overcommit 信息展示，但不会额外增加容量约束。</p>
        </article>

        <article class="tool-info-card">
          <span class="section-label">重要说明</span>
          <h2>这是容量规划估算。</h2>
          <p>实际调度还可能受到 node allocatable、kube-system 工作负载、DaemonSets、taints 和 tolerations、node affinity / anti-affinity、topology spread constraints、Pod max limits、hugepages、GPU 或其他 extended resources、ephemeral storage、不同节点规格以及剩余资源碎片等因素影响。结果不等同于 kube-scheduler 的精确行为。</p>
        </article>
      </section>

      <section class="tool-faq" aria-labelledby="faq-heading">
        <span class="section-label">FAQ</span>
        <h2 id="faq-heading">常见问题</h2>
        <details open>
          <summary>requests 和 limits 如何参与容量判断？</summary>
          <p>Kubernetes 调度在判断 Pod 是否能放入节点时使用 effective resource requests。已配置 request 时，本计算器使用该 request 判断容量；如果只配置了 limit，则使用该 limit 作为 effective request。除此之外，已配置的 limits 仅用于展示工作负载的规划和 overcommit 情况，不会额外增加容量约束。</p>
        </details>
        <details>
          <summary>规划预留代表什么？</summary>
          <p>规划预留用于为操作系统、kube-system、容器运行时、DaemonSet 以及日常运维预留容量。15% 是 InfraOpsX Calculator 的容量规划默认值，不是 Kubernetes 或 kubelet 的默认 systemReserved。该值仅用于容量规划，不代表 kubelet 的 systemReserved、kubeReserved 或实际 Node Allocatable。</p>
        </details>
        <details>
          <summary>结果是否等同于 kube-scheduler？</summary>
          <p>不等同。node allocatable、placement 规则、拓扑、extended resources、存储以及剩余资源的分布情况，都可能影响实际调度决定。</p>
        </details>
      </section>

      <section class="related-tools" data-pagefind-ignore>
        <div>
          <span class="section-label">相关工具</span>
          <h2>不确定 250m、512Mi 或 1Gi 分别代表什么？</h2>
        </div>
        <a class="button secondary" href="/zh/tools/kubernetes-quantity-converter/">打开 Kubernetes Quantity Converter</a>
      </section>

      <section class="tool-cta" data-pagefind-ignore>
        <div class="cta-box">
          <div>
            <span class="section-label">INFRAOPSX</span>
            <h2>需要 Kubernetes 容量规划方面的帮助？</h2>
            <p>联系 InfraOpsX，获取面向实际环境的基础设施建议。</p>
          </div>
          <a class="button primary" href="/zh/contact/">联系 InfraOpsX</a>
        </div>
      </section>
    </div>
  </main>

  <Footer locale="zh" />
</BaseLayout>
