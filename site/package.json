{
  "name": "infraopsx-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "test:quantity": "node --test tests/kubernetes-quantity.test.mjs"
  },
  "dependencies": {
    "astro": "latest",
    "@astrojs/sitemap": "latest",
    "@astrojs/rss": "latest"
  },
  "devDependencies": {
    "pagefind": "latest"
  }
}
